import { User } from './user';
import { RefreshToken } from './refreshToken';
import { Product, PriceSnapshot } from './products';
import { Alert } from './alert';

export const models = {
  User,
  RefreshToken,
  Product,
  PriceSnapshot,
  Alert
};

export { User, RefreshToken, Product, PriceSnapshot, Alert };
